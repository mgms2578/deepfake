# LLM 통합 호출 실험 보고서

## 목적

현재 구조는 사용자 입력마다 먼저 분류 LLM을 호출하고, 필요한 경우 다시 응답 생성 LLM을 호출한다.

이번 실험은 분류와 응답 생성을 하나의 LLM 호출로 합쳤을 때 속도, 비용, 품질 측면에서 이득이 있는지 확인하기 위한 것이다.

## 실험 방식

- 실험 모델: `gemini-2.5-flash-lite`
- 비교 대상:
  - 현재 구조: 분류 LLM 1회 + 응답 LLM 1회
  - 통합 후보: 응답 본문 + `[METADATA]` JSON을 한 번의 LLM 호출에서 생성
- 테스트 입력 8개:
  - `그래?`
  - `왜 100만 원이 필요한데?`
  - `100만 원은 너무 많고 50만 원만 가능해`
  - `계좌 줘`
  - `영상통화 해봐`
  - `ㅋㅋㅋ 계속 해봐`
  - `오늘 날씨 어때?`
  - `어`

실험 스크립트:

```text
backend/experiments/integrated-llm-experiment.js
```

## 결과 요약

| 항목 | 현재 2단계 | 통합 호출 |
| --- | ---: | ---: |
| 평균 첫 토큰 시간 | 1700ms | 710ms |
| 평균 완료 시간 | 1786ms | 950ms |
| 메타데이터 JSON 성공 | 안정적 | 8/8 성공 |

속도만 보면 통합 호출이 확실히 빠르다.

- 첫 토큰 기준 약 `990ms` 단축
- 완료 시간 기준 약 `836ms` 단축
- LLM 호출 횟수는 일반 턴 기준 2회에서 1회로 감소

## 품질 평가

통합 호출은 속도 이익은 크지만, 분류 품질이 흔들렸다.

### 정상 또는 개선된 케이스

`그래?`

- 현재 구조: `SCENARIO_RELATED / RELATED`
- 통합 호출: `SCENARIO_RELATED / RELATED`
- 결과: 정상

`왜 100만 원이 필요한데?`

- 현재 구조: `SCENARIO_RELATED / RELATED`
- 통합 호출: `SCENARIO_RELATED / RELATED`
- 결과: 정상

### 문제가 있는 케이스

`100만 원은 너무 많고 50만 원만 가능해`

- 현재 구조: `LOWER_AMOUNT_OFFER`
- 통합 호출: `RELATED`
- 문제: 사용자가 제시한 50만 원을 금액 제안으로 정확히 판정하지 못함

`계좌 줘`

- 현재 구조: `FULL_ACCEPTANCE`
- 통합 호출: `RELATED`
- 문제: 사기 성공 종료 조건을 놓침

`영상통화 해봐`

- 현재 구조: `REFUSAL_OR_DEFENSE`
- 통합 호출: `RELATED`
- 문제: 신원 확인/방어 행동을 제대로 잡지 못함

`ㅋㅋㅋ 계속 해봐`

- 현재 구조: `UNRELATED / STALLING_OR_MOCKING`
- 통합 호출: `RELATED`
- 문제: 조롱/지연 행위를 놓침

`오늘 날씨 어때?`

- 현재 구조: `UNRELATED / OFF_TOPIC`
- 통합 호출: `RELATED`
- 문제: 무관 발화를 시나리오 관련 발화로 오판

`어`

- 현재 구조: `CONFUSED_OR_NOISE`
- 통합 호출: `RELATED`
- 문제: 실제 noise를 잡지 못함

## 비용 평가

통합 호출은 일반 대화 기준 LLM 호출 횟수를 줄인다.

```text
현재: 분류 1회 + 응답 1회
통합: 응답/분류 통합 1회
```

따라서 호출 횟수 기준으로는 최대 50% 감소 가능성이 있다.

다만 실제 비용은 50%까지 줄지 않는다.

- 통합 프롬프트가 더 길어진다.
- 응답 생성 모델이 분류 작업까지 같이 수행한다.
- 메타데이터 형식 강제 지시가 추가된다.

현실적인 LLM 비용 절감 기대치는 약 `20~40%` 수준으로 보는 것이 맞다.

## 결론

현재 상태에서 통합 호출을 바로 운영 적용하는 것은 권장하지 않는다.

이유:

- 속도는 약 0.8~1.0초 개선된다.
- 메타데이터는 강한 출력 형식 지시로 8/8 확보 가능했다.
- 하지만 핵심 판정 품질이 불안정하다.
- 특히 `계좌 줘`, `50만 원만 가능해`, `영상통화 해봐`, `오늘 날씨 어때?`, `어` 같은 운영 핵심 케이스에서 오판이 발생했다.

이 서비스는 단순 챗봇이 아니라 결과 판정, 송금 성공/방어 성공/사기범 포기 종료가 중요하다.
따라서 분류 품질이 흔들리면 결과 페이지 신뢰도가 떨어진다.

## 권장 방향

운영 적용은 현재 2단계 구조를 유지하는 것이 안전하다.

다만 속도 개선을 위해 다음 순서로 추가 실험을 권장한다.

1. 통합 호출 프롬프트를 더 세밀하게 개선
2. 메타데이터 JSON을 별도 후처리 검증
3. 위험 케이스는 서버에서 재검증
4. 통합 호출 결과가 `PAYMENT_OFFER`, `VERIFICATION_OR_DEFENSE`, `OFF_TOPIC`, `CONFUSED_OR_NOISE`를 안정적으로 맞추는지 재평가
5. 통합 호출을 전면 적용하지 말고 일부 안전 케이스부터 A/B 테스트

최종 판단:

```text
속도/비용 이익은 있음.
하지만 현재 품질로는 운영 적용 불가.
현재는 2단계 유지가 맞고, 통합 호출은 추가 프롬프트 개선 실험 대상으로 남기는 것이 적절함.
```
